name: Build & Release Electron App

on:
  push:
    branches: [ master, 'release-candidate*', 'feature-*' ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-14, ubuntu-latest, windows-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          submodules: true
          lfs: true

      - name: Cache Yarn
        uses: actions/cache@v3
        with:
          path: ~/.yarn/cache
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: ${{ runner.os }}-yarn-

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version-file: '.nvmrc'

      - name: Yarn Install & Build
        run: |
          yarn install --frozen-lockfile
          yarn build

      - name: Debug Apple Notarization Vars
        run: |
          echo "APPLE_API_KEY length: ${#APPLE_API_KEY}"
          echo "APPLE_API_KEY_ID: $APPLE_API_KEY_ID"
          echo "APPLE_API_ISSUER: $APPLE_API_ISSUER"
          if [ -z "$APPLE_API_KEY" ] || [ -z "$APPLE_API_KEY_ID" ] || [ -z "$APPLE_API_ISSUER" ]; then
            echo "❌ One or more Apple notarization secrets are missing or empty!"
            exit 1
          fi
        env:
          APPLE_API_KEY: ${{ secrets.apple_api_key }}
          APPLE_API_KEY_ID: ${{ secrets.apple_api_key_id }}
          APPLE_API_ISSUER: ${{ secrets.apple_api_issuer }}

      - name: Package Electron
        run: |
          yarn run release \
            --publish never \
            --config.mac.notarize=true \
            --config.mac.hardenedRuntime=true \
            --config.mac.entitlements=entitlements.mac.plist
        env:
          GH_TOKEN: ${{ secrets.github_token }}
          CSC_LINK: ${{ secrets.mac_certs }}
          CSC_KEY_PASSWORD: ${{ secrets.mac_certs_password }}
          APPLE_API_KEY: ${{ secrets.apple_api_key }}
          APPLE_API_KEY_ID: ${{ secrets.apple_api_key_id }}
          APPLE_API_ISSUER: ${{ secrets.apple_api_issuer }}

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.os }}
          path: packages/keepkey-desktop/dist/

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v3
        with:
          path: ./dist

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/') || github.ref == 'refs/heads/master'
        with:
          files: ./dist/**
        env:
          GITHUB_TOKEN: ${{ secrets.github_token }}
